<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dk.netdesign.mybatis.extender.sample.sqllite.mappers.SampleXMLBasedMapper">
    <select id="sayHello" resultType="String">
        SELECT datetime('now')
    </select>
    <select id="getAllTables" resultType="String">
        SELECT datetime('now')
        UNION SELECT 1
        UNION SELECT 2
        UNION SELECT 3
        UNION SELECT 4
        UNION SELECT 5
        UNION SELECT 6
    </select>


</mapper>